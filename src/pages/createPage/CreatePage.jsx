import React from "react";
import CreateAdventureCard from "../../components/createAdventureCard/CreateAdventureCard";

const CreatePage = () => {
    return (
        <div>
            <h2>This is where you will create a new adventure card to add to your page or the database</h2>
            <CreateAdventureCard/>
        </div>
    )
};

export default CreatePage;