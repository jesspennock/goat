import React from "react";
import CreateAdventureCard from "../../components/createAdventureCard/CreateAdventureCard";

const CreatePage = () => {
    return (
        <div>
            <h2>Get started creating your own adventure cards to document and share</h2>
            <CreateAdventureCard/>
        </div>
    )
};

export default CreatePage;